# Live AI and YES24 connection

User authorizes connecting actual provider keys and exercising real search/AI requests. Keys were absent at initial inspection; user has been asked to place OpenAI key/model in ignored worker .env and YES24 key in ignored web .env.local, or provide their existing file location. Never print credentials. Keep local PDF worker and existing accounts intact. Leave implementation uncommitted.

## Design

Use existing OpenAI Responses strict-schema worker; enable AI only after key/model configuration is present and validated. Validate one book analysis and one coaching request through actual Auth/Next/queue/worker/provider. Report real provider errors without raw responses, and distinguish fixture tests from live verification. No automatic model selection or provider substitution.

Implement YES24 official GET https://apis.yes24.com/v1/goods/itemList using server-only X-Api-Key, query/category=BOOK/detail=Y/page/pageSize. Map documented success/data.items/totalCount and pages,author,isbn13/isbn10,cover,contentDetail metadata into BookMetadata. Use bounded response reading, timeout/no redirects, safe unavailable errors, SEARCH_001 empty result. No guessed page count. Support existing offset contract with page-aware slicing, at most two requests. Fixed host; keys never enter URL or browser. Source YES24 with positive numeric product sourceId validates and persists through existing BOOK registration (source is text).

Expose YES24/Google selector in existing book search, source attribution and original product link generated from validated numeric ID. Switch provider clears stale results and selected source; preserve manual entry. Search remains explicit; no background paid calls. No new database or dependency required. Metadata contents can be shown as text but automatic storage/AI enrichment is a separate choice; current explicit AI outline input remains usable.

## Tasks

- [x] Provider worker: package implementation with red/green tests; source validation and sanitized error/body limits/pagination.
- [x] Root: search handler env injection and regression expectations; web provider selector, env examples and docs.
- [x] Root: confirm keys are configured without printing values; enable worker/web AI, live YES24 search and registration then AI analysis/coaching. Cleanup only temporary test account/data. If keys remain missing, report exact configuration needed and avoid claiming live success.
- [x] Final review/type/lint/build/unit/actual integration checks appropriate to changes, secret exclusion and git status.

Preparation verification: Node 97 + Scheduler 52 tests passed; lint/build passed; actual Auth/production Next/DB registration test including YES24 provenance and foreign-owner denial passed. Live script stops before network calls when required credentials are missing. YES24/OpenAI credentials and model were absent at inspection, so live provider calls and browser real-result verification remain pending. No AI flags were enabled and no provider keys were written by the agent.

Live follow-up 2026-09-13: user supplied both keys and authorized model selection. Official OpenAI documentation checked; configured gpt-5.4-mini and enabled web/worker AI while preserving PDF settings. Recreated local worker. `pnpm test:providers:live` passed: actual YES24 10 results, selected 784-page book registered, BOOK_ANALYSIS and COACH completed with validated structured output. Temporary account/data removed. Keys remain only in ignored environment files. Broad analysis quality and browser display of these live outputs were not evaluated by this check.
