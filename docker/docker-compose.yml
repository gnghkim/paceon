name: paceon

services:
  ai-worker:
    build:
      context: ../services/ai-worker
    ports:
      - "127.0.0.1:8000:8000"
    init: true
    stop_grace_period: 180s
    environment:
      AI_ENABLED: ${AI_ENABLED:-false}
      PDF_ENABLED: ${PDF_ENABLED:-false}
      SUPABASE_URL: ${SUPABASE_URL:-http://host.docker.internal:55321}
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-}
      OPENAI_TRANSCRIBE_MODEL: ${OPENAI_TRANSCRIBE_MODEL:-gpt-4o-mini-transcribe}
      OPENAI_TTS_MODEL: ${OPENAI_TTS_MODEL:-gpt-4o-mini-tts}
      OPENAI_TTS_VOICE: ${OPENAI_TTS_VOICE:-marin}
      AI_POLL_SECONDS: ${AI_POLL_SECONDS:-3}
      # 매일 알림. 키가 비어 있으면 발송기는 아예 돌지 않고 나머지는 그대로 동작한다.
      VAPID_PUBLIC_KEY: ${VAPID_PUBLIC_KEY:-}
      VAPID_PRIVATE_KEY: ${VAPID_PRIVATE_KEY:-}
      VAPID_SUBJECT: ${VAPID_SUBJECT:-}
      NOTIFY_POLL_SECONDS: ${NOTIFY_POLL_SECONDS:-60}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3)"]
      interval: 10s
      timeout: 5s
      start_period: 10s
      retries: 5
